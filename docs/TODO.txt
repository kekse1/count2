
	@ *.md: bitte links zu den php-klassen/-dateien anpassen zu den 'docs/*/*.md', und dort jew. am anfang link zu den *.php files itself!

		# die file-links direkt unter dem (neuen) 'Back to Index'-link! falls der noch fehlt somewhere, bitte zusaetzlich nachruesten! thx.

	# if necessary, use 'php://memory' and 'php://temp'!!!

	# 'php://input': returns all the raw data of the http request (after the header)

			< https://stackoverflow.com/questions/8893574/php-php-input-vs-post >

		@ Parameter: guck mal, ob das speziell parsen, also $_POST dazu statt $_GET?!??

	# *moeglichst alle* freistehenden funktionen in extra klassen!!

		# und dazu den autoloader..?! aber beachte die syntax.. evtl. alles w/ namespace dann!?

	# `Security`(.php) [w/ `String`, U.A.]: strategien genau ausarbeiten..!!

		# u.v.a.. meiszte fs-zugriffe nur mit `FileSystem`-INSTANZ [wg. chroot ;-] ...

			# force root path (nicht nur check if); also use 'Path::normalize(..., $preserve = false)';

		# ueberlege auch wg. 'Security::secure()', u.a. mit 'Path::secure()'... wg. filesystem-chroot sowieso....
	
	@ 'Connection'/'terminal'/...:

		# Connection::flush() etc. is ready, but merely *untested*..
	
	 		# and don't know if true/false as default, see 'KEKSE_CONNECTION_BUFFER' @ constants.inc.php

				# but can't be too bad (in most environments!!), since .flush() @ 'register_shutdown_function()'! ;-)

					# so YES, set it as default (e.g. one can change header variables afterwards! ;-)

		# eine art 'context'-buffer evtl..? auch fuer meine js-lib, etc.. evtl.?

			# sprich, sammeln von ausgabe-daten in context(s), ..
		       
				# .. um keine weitere, nicht zusammenhangende ausgabe "mittendrin" zu erzeugen..

				# .. und um je nachdem auch ausgabe (@ flush()) zu gruppieren? je nachdem [priority] evtl., o.ae..

			# evtl. extra 'Context'-klasse? @ stdio w/ [INPUT,OUTPUT,ERROR];

	@ 'kekse/constants.php': weitere content types, und dazu noch @ 'Quant' dazu..

		# evtl. dynamischer kosntruiert? dass quasi 'addContentType()' o.ae., etc..!?!

	# map: set() mit $scheme!?

		# und values mit strings und cast oder direkt weitere types??

		# bisschen noch ueberdenken alles besser..

		@ scheme: ist 'number' als type erlaubt?!? SOLLTE EIG.!!! ;-D

 	# alle (string) zu (int) sowie (double) casts EVTL.(!!) eher durch 'parse()', etc.. @ numeric.php..?!?

		# kann ein cast-operator ueberladen werden?

		# ABER: eher nicht.. cast sollte effizienter sein!!

			# gucke nur bei strings, ob der radix 10 ist!!

	@ 'count2/counting.php': STORE BINARY VALUES [brauchen ja kein '-'/'+'/'.'... somit optimaler bytecode-radix ;-]

	@ FINGERPRINT: alle BOOKMARKS dazu sowie 'canvas.pdf' etc. zu rate heran ziehen noch.. ^_^

	# bitte denke an kekse/ w/ library-styles.. paar nette funktionen, aber nicht zu sehr aufblaehen.. thx. ^_^

		# gucke, was alles noetig ist fuer alle module.. moeglichst nur wirklich verwendetes..

	# '[uu]id.php' is still TODO!

	# terminal, console(.{log,info,..}), ansi, getopt, ... etc. ... alles fuer CLI mode.

		# diesmal mit 'Text::table()' (um bessere tabellen-ansicht im CLI-mode v.a..!)!!

		# dazu noch 'Text::bold()' und alle weiteren.. je nach modus entweder ansi escape seq. oder html.. falls nicht type=='text'!

			# bedenke: 'Text::{less,strlen}()' steht jetzt auch zur verfuegung..

				# gerade bei tabellen brauche ich die pure text-laenge, ohne jede ansi-esc-seq. o.ae..

				# und don't forget: ansi esc. seq. sollten bei mir mit \0 enden, allesamt [gibt keine bessere loesung, so ganz offiziell];

	# VOR ALLEM @ "click count(er)"!!!! direkt in meinem `v4` nutzen dann, pls..!!!

		@ 'page.js': automatische <a>-innerHTML-erweiterung um die zahlen..! :-D


