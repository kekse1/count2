
	# importValues*()? dann bitte pure werte, kein schema w/ type/def./..

		@ "echte" config.json's! w/ pure values!!!

	# config und param .json anpassen und keep in sync

	# initiale casts @ map via scheme!

		# wenn später scheme, gleich alle values prüfen

		# evtl. nur relays der .getBoolean() etc. zu .get(..., 'boolean'); etc..?

