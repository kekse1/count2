<img src="https://kekse.biz/github.php?draw&override=github:count2">

[**Back to Index**](../kekse.md)

# `Path`
Often used by the [`FileSystem`](filesystem.md).

## Functions
* **`Path::secure($path)`**
* **`Path::getRoot($real = KEKSE_PATH_REAL)`**
* **`Path::resolve(... $args)`**
* **`Path::join(... $args)`**
* **`Path::normalize($path)`**
* **`Path::extname($path, $count = KEKSE_PATH_EXT)`**
* **`Path::relative($from, $to)`**

